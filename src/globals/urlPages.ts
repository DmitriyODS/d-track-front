export enum UrlPages {
  Claims = '/claims',
  Customers = '/customers',
  Employees = '/employees',
  ProcessesClaims = '/processes-claims',
  TaskBoard = '/task-board',
  Tasks = '/tasks',
  Auth = '/auth',
}
